"""Placeholder so CI has something to run before the engine test suite
lands (pytest exits nonzero on zero collected tests). Delete this once
real tests exist in this directory.
"""


def test_placeholder():
    assert True
